# N.O.V.A. Doctor Agent (nova_agent/ + competition/ + evaluation/) runtime dependencies.
#
# Deliberately separate from backend/requirements*.txt: nova_agent only reuses a few pure-Python
# modules from the vendored SynexAgent backend (app.schemas, app.services.audit/vitals/
# terminology_mapper -- see nova_agent/_synex.py), none of which need FastAPI, ONNX Runtime,
# Redis, or any of the other backend/requirements-core.txt packages. Competition submission and
# local evaluation/benchmarking therefore never need to install the full backend stack.
pydantic>=2.6,<3
pytest>=8.0

# Optional: only required when NOVA_LLM_PROVIDER=anthropic (see nova_agent/config.py and
# nova_agent/llm_client.py). The default NOVA_LLM_PROVIDER=mock runs fully offline without it.
# anthropic>=0.40
